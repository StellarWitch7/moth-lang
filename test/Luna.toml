name = "test"
version = "1.0"
type = "exe"
root = "main"
out = "build"
include = "include"
target-platforms = [ "linux", "windows" ]
target-languages = [ "c" ]

[dependencies.project]
moth-core = { dir = "/home/aur/MothProjects/moth-core" }