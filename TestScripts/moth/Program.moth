namespace default.test;

func main() #i32 {
    local thing #Thing = #Thing.init();
    local string #str = #Sys.Input(thing.string);
    #Sys.Print(string);

    local string2 #str = #Sys.Input("Enter anything to exit: ");
    free(string2);
    return 0;
}

public class Thing {
    public string #str;

    static init() #Thing {
        self.string = "Hello world! Enter something please: ";
        return self;
    }
}