namespace Test;

func main() #i32 {
    #Sys.WriteLine("Welcome!");
    #Sys.Input("Enter something: ");
    #Sys.WriteLine("Goodbye!");

    local chars ?= #Array<\#char, 4\>.init();
    chars.SetIndex(0, '\n');

    if chars.GetIndex(ref local val #char) {
        return #i32 <- val;
    } else {
        return 1;
    }
}