namespace test;

with core;
with core::io;
with core::threading;

func main() #i32 {
    local thread #Thread = #Thread.init(func(arg #void*) #void* {
        STDOut.WriteLine("Hello threaded world!");
        return null;
    }, null);
    thread.Start();
    thread.Join();

    // Exit();

    while true {

    }

    return 0;
}
